# --- LLM (GPT-5.5) ---
LLM_URL=
LLM_API_KEY=
LLM_MODEL=

# --- Azure OpenAI (GPT-4.1) ---
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT_NAME=

# --- Postgres (service Docker Compose, non utilise si DATABASE_URL pointe vers Supabase) ---
POSTGRES_USER=chiffra
POSTGRES_PASSWORD=chiffra
POSTGRES_DB=chiffra

# --- Database (Supabase) ---
# Coller ici la connection string donnee par Supabase (Project Settings > Database > Connection string).
# Exemple : postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
DATABASE_URL=

# --- Redis ---
REDIS_URL=
